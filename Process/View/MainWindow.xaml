<Window x:Class="Process.View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Process.View"
        xmlns:viewModels="clr-namespace:Processes.ViewModels"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <HierarchicalDataTemplate DataType ="{x:Type viewModels:ProcessViewModel}" ItemsSource = "{Binding Path = Processes}" >
            <TextBlock Foreground="Green" Text="{Binding Path= ProcessName}"/>
        </HierarchicalDataTemplate>
    </Window.Resources>

    <Grid>
        <TreeView ItemsSource = "{Binding Path = Processes}"/>
    </Grid>
</Window>